#!/usr/bin/env python3
"""Split original_draft.md into individual chapter files."""

import os
import re
import unicodedata


def slugify(text):
    """Convert text to a filesystem-friendly slug."""
    text = text.strip().lower()
    text = text.replace("&", "and")
    text = text.replace("'", "")
    text = text.replace('"', "")
    text = text.replace("'", "")
    text = text.replace("\u2019", "")
    text = re.sub(r"[^\w\s-]", "", text)
    text = re.sub(r"[-\s]+", "_", text)
    text = text.strip("_")
    return text


def make_frontmatter(title, part, section, start_line, end_line):
    return (
        f"---\n"
        f'title: "{title}"\n'
        f"part: {part}\n"
        f"section: {section}\n"
        f'original_lines: "{start_line}-{end_line}"\n'
        f"---\n\n"
    )


def write_chapter(filepath, frontmatter, content_lines):
    os.makedirs(os.path.dirname(filepath), exist_ok=True)
    content = "\n".join(content_lines).strip()
    if not content:
        return
    with open(filepath, "w") as f:
        f.write(frontmatter)
        f.write(content)
        f.write("\n")


def detect_boundaries(lines):
    """Detect all structural boundaries in the manuscript."""
    boundaries = []  # (line_idx, type, raw_text)

    for i, line in enumerate(lines):
        stripped = line.strip()

        # Section headers (in body, after TOC)
        if i >= 424 and re.match(r"\*\*SECTION \d", stripped):
            boundaries.append((i, "section", stripped))

        # Part headers (in body)
        if i >= 424 and re.match(r"\*\*PART \d", stripped):
            boundaries.append((i, "part", stripped))

        # Chapter headers
        if re.match(r"\*\*CHAPTER\s*:", stripped):
            boundaries.append((i, "chapter", stripped))

        # Annexure headers
        if re.match(r"\*\*ANNEXURE:", stripped):
            boundaries.append((i, "annexure", stripped))

        # Good practices sections
        if re.match(r"\*\*(SOME GOOD PRACTICES|Some good practices)\*\*", stripped):
            if i >= 424:
                boundaries.append((i, "good_practices", stripped))

    return boundaries


def extract_title_from_bold(text):
    """Extract title from **BOLD TEXT** pattern."""
    m = re.match(r"\*\*(.+?)\*\*", text.strip())
    if m:
        return m.group(1).strip()
    return text.strip().strip("*").strip()


def split_manuscript():
    with open("original_draft.md", "r") as f:
        raw = f.read()
    lines = raw.split("\n")
    total_lines = len(lines)

    index_entries = []

    # --- Frontmatter (lines 1-424) ---
    frontmatter_sections = [
        ("introduction", "Introduction to the Book", 345, 348),
        ("preface", "Preface", 351, 393),
        ("about_the_author", "About the Author", 395, 405),
        ("acknowledgement", "Acknowledgement", 407, 415),
        ("disclaimer", "Disclaimer", 417, 423),
    ]

    # Find actual line ranges by searching
    fm_markers = {
        "introduction": (r"\*\*INTRODUCTION TO THE BOOK\*\*", r"\*\*PREFACE\*\*"),
        "preface": (r"\*\*PREFACE\*\*", r"\*\*ABOUT THE AUTHOR\*\*"),
        "about_the_author": (r"\*\*ABOUT THE AUTHOR\*\*", r"\*\*ACKNOWLEDGEMENT\*\*"),
        "acknowledgement": (r"\*\*ACKNOWLEDGEMENT\*\*", r"\*\*DISCLAIMER\*\*"),
        "disclaimer": (r"\*\*DISCLAIMER\*\*", r"\*\*SECTION 1 – LEADERSHIP\*\*"),
    }

    # Find frontmatter boundaries in the body (after TOC, line ~345+)
    fm_positions = {}
    for i, line in enumerate(lines):
        if i < 340:
            continue
        stripped = line.strip()
        if stripped == "**INTRODUCTION TO THE BOOK**":
            fm_positions["introduction_start"] = i
        elif stripped == "**PREFACE**":
            fm_positions["preface_start"] = i
        elif stripped == "**ABOUT THE AUTHOR**":
            fm_positions["about_the_author_start"] = i
        elif stripped == "**ACKNOWLEDGEMENT**":
            fm_positions["acknowledgement_start"] = i
        elif stripped == "**DISCLAIMER**":
            fm_positions["disclaimer_start"] = i
        elif i >= 420 and re.match(r"\*\*SECTION 1", stripped):
            fm_positions["section1_start"] = i
            break

    os.makedirs("chapters/00_frontmatter", exist_ok=True)

    fm_order = ["introduction", "preface", "about_the_author", "acknowledgement", "disclaimer"]
    fm_titles = {
        "introduction": "Introduction to the Book",
        "preface": "Preface",
        "about_the_author": "About the Author",
        "acknowledgement": "Acknowledgement",
        "disclaimer": "Disclaimer",
    }
    fm_ends = {
        "introduction": "preface_start",
        "preface": "about_the_author_start",
        "about_the_author": "acknowledgement_start",
        "acknowledgement": "disclaimer_start",
        "disclaimer": "section1_start",
    }

    for idx, key in enumerate(fm_order):
        start = fm_positions[f"{key}_start"]
        end = fm_positions[fm_ends[key]]
        content = lines[start:end]
        filename = f"{idx+1:02d}_{key}.md"
        filepath = f"chapters/00_frontmatter/{filename}"
        fm = make_frontmatter(fm_titles[key], 0, 0, start + 1, end)
        write_chapter(filepath, fm, content)
        index_entries.append(f"- [00_frontmatter/{filename}]({filename}) - {fm_titles[key]}")

    # --- Body sections ---
    # Map sections to folder names
    section_folders = {
        1: "01_leadership",
        2: "02_people",
        3: "03_operations",
        4: "04_support_services",
        5: "05_articles",
    }

    # Map part numbers to folder names (from TOC)
    part_info = {
        1: ("part_01_cxo_leadership", "CXO/SBU/BU/Department Head Level Leadership"),
        2: ("part_02_employees", "People, the Employees"),
        3: ("part_03_strategy", "Strategy"),
        4: ("part_04_marketing", "Marketing & Business Development"),
        5: ("part_05_advertising", "Advertising"),
        6: ("part_06_designs_engineering", "Designs & Engineering"),
        7: ("part_07_bidding_costing", "Bidding & Costing"),
        8: ("part_08_project_management", "Project Management"),
        9: ("part_09_site_operations", "Site Operations"),
        10: ("part_10_production_management", "Production Management"),
        11: ("part_11_supply_chain", "Supply Chain Management"),
        12: ("part_12_fleet_equipment", "Fleet & Equipment Management"),
        13: ("part_13_stores_management", "Stores Management"),
        14: ("part_14_quality", "Quality"),
        15: ("part_15_safety", "Safety"),
        16: ("part_16_hr", "HR"),
        17: ("part_17_administration", "Administration"),
        18: ("part_18_contracts_law", "Contracts & Law"),
        19: ("part_19_finance_accounts", "Finance & Accounts"),
        20: ("part_20_audit", "Audit"),
        21: ("part_21_information_technology", "Information Technology"),
        22: ("part_22_learning_development", "Learning & Development"),
        23: ("part_23_insurance", "Insurance"),
        24: ("part_24_ethics", "Ethics"),
        25: ("part_25_esg", "ESG"),
        26: ("part_26_csr", "CSR"),
        27: ("part_27_new_concepts", "New Concepts"),
        28: ("part_28_annexures", "Annexures"),
        29: ("part_29_personal_advice", "Personal Advice"),
        30: ("part_30_my_learning", "My Learning"),
    }

    part_to_section = {}
    for p in range(1, 2):
        part_to_section[p] = 1
    for p in range(2, 3):
        part_to_section[p] = 2
    for p in range(3, 16):
        part_to_section[p] = 3
    for p in range(16, 27):
        part_to_section[p] = 4
    for p in range(27, 31):
        part_to_section[p] = 5

    # Find part boundaries in body
    part_starts = {}
    for i, line in enumerate(lines):
        if i < 424:
            continue
        stripped = line.strip()
        # Match patterns like **PART 1**, **PART 28 : ANNEXURES**, **PART 29: PERSONAL ADVICE**
        m = re.match(r"\*\*PART (\d+)", stripped)
        if m:
            pnum = int(m.group(1))
            part_starts[pnum] = i

    # Sort parts by their start position
    sorted_parts = sorted(part_starts.items(), key=lambda x: x[1])

    # Process each part
    for pi, (pnum, pstart) in enumerate(sorted_parts):
        # Determine end of this part
        if pi + 1 < len(sorted_parts):
            # Check if there's a section header between this part and next
            pend = sorted_parts[pi + 1][1]
            # Walk back past any section headers
            for si, line in enumerate(lines):
                if si >= pstart and si < pend and si > pstart + 2:
                    if re.match(r"\*\*SECTION \d", line.strip()):
                        pend = si
                        break
        else:
            pend = total_lines

        sec = part_to_section[pnum]
        sec_folder = section_folders[sec]
        part_folder_name, part_title = part_info[pnum]
        part_dir = f"chapters/{sec_folder}/{part_folder_name}"
        os.makedirs(part_dir, exist_ok=True)

        # Get all lines for this part
        part_lines = lines[pstart:pend]

        # Find chapter/annexure/topic boundaries within this part
        chapters_in_part = []

        for li, line in enumerate(part_lines):
            abs_line = pstart + li
            stripped = line.strip()

            if li < 2:
                continue  # Skip the part header itself

            if re.match(r"\*\*CHAPTER\s*:", stripped):
                title = re.sub(r"\*\*CHAPTER\s*:\s*", "", stripped).rstrip("*").strip()
                chapters_in_part.append((li, "chapter", title, abs_line))
            elif re.match(r"\*\*ANNEXURE:", stripped):
                title = re.sub(r"\*\*ANNEXURE:\s*", "", stripped).rstrip("*").strip()
                chapters_in_part.append((li, "annexure", title, abs_line))
            elif re.match(r"\*\*(SOME GOOD PRACTICES|Some good practices)\*\*", stripped):
                chapters_in_part.append((li, "good_practices", "Some Good Practices", abs_line))
            elif pnum == 29 and li > 2 and re.match(r"\*\*[A-Z][A-Z\s&'\u2019\u2018-]+\*\*$", stripped):
                # Part 29 bold topic headers (all caps)
                title = extract_title_from_bold(stripped)
                # Skip if it's just a one-line bold statement within a topic
                if len(title) > 3 and title not in ("ANNEXURES",):
                    chapters_in_part.append((li, "topic", title, abs_line))
            elif pnum == 30 and li >= 2:
                # Part 30 bold topic headers (mixed case)
                if re.match(r"\*\*[A-Z][a-zA-Z &,'-]+\*\*\s*$", stripped):
                    title = extract_title_from_bold(stripped)
                    if len(title) > 3:
                        chapters_in_part.append((li, "topic", title, abs_line))
            # Non-chapter bold headers for force-your-style, deliberate-procrastination pattern
            elif pnum <= 26 and li > 2 and not re.match(r"\*\*(SOME GOOD|Some good|The role|CHAPTER|ANNEXURE|PART|SECTION)", stripped):
                if re.match(r'\*\*THE "[A-Z\s]+" SYNDROME\*\*', stripped, re.IGNORECASE):
                    title = extract_title_from_bold(stripped)
                    chapters_in_part.append((li, "chapter", title, abs_line))
                elif re.match(r"\*\*THE .+ SYNDROME\*\*", stripped, re.IGNORECASE):
                    title = extract_title_from_bold(stripped)
                    chapters_in_part.append((li, "chapter", title, abs_line))

        # Determine intro content (between part header and first chapter)
        intro_end = chapters_in_part[0][0] if chapters_in_part else len(part_lines)

        # Find where the actual intro content starts (skip part header lines)
        intro_start = 0
        for li in range(min(intro_end, len(part_lines))):
            stripped = part_lines[li].strip()
            if re.match(r"\*\*PART \d", stripped):
                continue
            if not stripped:
                continue
            # Found first content line
            intro_start = li
            break

        # Write intro if there's content
        intro_content = part_lines[intro_start:intro_end]
        # Check if there's actual content (not just blank lines)
        has_intro_content = any(l.strip() for l in intro_content)
        if has_intro_content:
            intro_filepath = f"{part_dir}/00_intro.md"
            fm = make_frontmatter(
                f"Introduction - {part_title}",
                pnum, sec,
                pstart + intro_start + 1,
                pstart + intro_end
            )
            write_chapter(intro_filepath, fm, intro_content)
            rel_path = f"{sec_folder}/{part_folder_name}/00_intro.md"
            index_entries.append(f"- [{rel_path}]({rel_path}) - Introduction: {part_title}")

        # Write each chapter/topic
        for ci, (local_start, ctype, title, abs_start) in enumerate(chapters_in_part):
            if ci + 1 < len(chapters_in_part):
                local_end = chapters_in_part[ci + 1][0]
            else:
                local_end = len(part_lines)

            content = part_lines[local_start:local_end]

            # Generate filename
            file_idx = ci + 1
            slug = slugify(title)
            if len(slug) > 60:
                slug = slug[:60].rstrip("_")

            if ctype == "good_practices":
                filename = f"99_good_practices.md"
                # Check if there's already a 99_good_practices - use different number
                existing = [c for c in chapters_in_part[:ci] if c[2] == "Some Good Practices"]
                if existing:
                    filename = f"99b_good_practices.md"
            elif ctype == "annexure":
                filename = f"{file_idx:02d}_{slug}.md"
            else:
                filename = f"{file_idx:02d}_{slug}.md"

            filepath = f"{part_dir}/{filename}"
            fm = make_frontmatter(title, pnum, sec, abs_start + 1, pstart + local_end)
            write_chapter(filepath, fm, content)
            rel_path = f"{sec_folder}/{part_folder_name}/{filename}"
            prefix = "Annexure" if ctype == "annexure" else "Chapter"
            if ctype == "good_practices":
                prefix = ""
                display = "Some Good Practices"
            elif ctype == "topic":
                prefix = ""
                display = title
            else:
                display = title
            label = f"{prefix}: {display}" if prefix else display
            index_entries.append(f"- [{rel_path}]({rel_path}) - {label}")

    # Write index
    with open("chapters/index.md", "w") as f:
        f.write("# Book Index - Avoid Misgovernance in Corporates\n\n")
        f.write("## Reading Order\n\n")
        for entry in index_entries:
            f.write(entry + "\n")

    print(f"Split complete. {len(index_entries)} files written.")
    print("Index written to chapters/index.md")


if __name__ == "__main__":
    split_manuscript()
